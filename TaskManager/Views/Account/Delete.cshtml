@{
    ViewBag.Title = "Delete Account";
}

<div class="col-md-4 col-md-offset-4">
    <h2 class="inacc text-center">Are you sure you want to delete account?</h2>
    @using (Html.BeginForm("Delete", "Account", new { role = "form" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-group">
            <button type="submit" class="delbtn btn btn-danger">Yes, delete!</button>
        </div>
    }
</div>